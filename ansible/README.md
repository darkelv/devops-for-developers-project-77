# Ansible

В этой директории будут Ansible-плейбуки и переменные для настройки серверов.

Файл `group_vars/all/terraform_vault.yml` хранит зашифрованные переменные, которые нужны Terraform для работы с облаком.
